<?php
// Placeholder: Composer generates this file during `composer install`.
// If you see this, run: composer install (or docker compose build)
throw new \RuntimeException(
    'Composer autoloader not installed. Run "composer install" or "docker compose build".'
);
